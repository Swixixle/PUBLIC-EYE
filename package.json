{
  "name": "frame",
  "version": "0.1.0",
  "private": true,
  "description": "Frame — transparency receipts for political claims",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-keys": "tsx scripts/generate-keys.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "canonicalize": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
